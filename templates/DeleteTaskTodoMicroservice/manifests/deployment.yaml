apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-todo-delete-tasks-api
spec:
  replicas: {{ .Values.deletetasks.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-todo-delete-tasks-api
  template:
    metadata:
      name: {{ .Release.Name }}-todo-delete-tasks
      labels:
        app: {{ .Release.Name }}-todo-delete-tasks-api
    spec:
      containers:
       - name: {{ .Release.Name }}-todo-delete-tasks-api
         image: {{ .Values.common.acr_name }}/{{ .Values.deletetasks.image.image_name }}:{{ .Values.deletetasks.image.tag }}
         ports:
          - containerPort: {{ .Values.deletetasks.image.ports.containerPort }}
         env: 
           - name: {{ .Values.common.env.name }}
             value: {{ .Values.common.env.value }}
