apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-todo-delete-tasks-api
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-todo-delete-tasks-api
  ports:
    - port: {{ .Values.deletetasks.ports.port }}
      targetPort: {{ .Values.deletetasks.ports.targetPort }}
      
