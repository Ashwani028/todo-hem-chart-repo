apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-todo-get-task-api
spec:
  replicas: {{ .Values.gettasks.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-todo-get-task-api
  template:
    metadata:
      name: {{ .Release.Name }}-todo-get-task
      labels:
        app: {{ .Release.Name }}-todo-get-task-api
    spec:
      containers:
      - name: {{ .Release.Name }}-todo-get-task-api
        image: {{ .Values.common.acr_name }}/{{ .Values.gettasks.image.image_name }}:{{ .Values.gettasks.image.tag }}
        ports:
        - containerPort: {{ .Values.gettasks.ports.containerPort }}
        env: 
        - name: {{ .Values.common.env.name }}
          value: {{ .Values.common.env.value }}

