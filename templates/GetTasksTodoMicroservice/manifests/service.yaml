apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-todo-get-task-api
spec:
  type: {{ .Values.gettasks.type }}
  selector:
    app: {{ .Release.Name }}-todo-get-task-api
  ports:
    - port: {{ .Values.gettasks.ports.port }}
      targetPort: {{ .Values.gettasks.ports.targetPort }}
      
