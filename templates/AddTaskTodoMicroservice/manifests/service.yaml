apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-todo-add-tasks-api
spec:
  type: {{ .Values.addtasks.type }}
  selector:
    app: {{ .Release.Name }}-todo-add-tasks-api
  ports:
   - port: {{ .Values.addtasks.ports.port }}
     targetPort: {{ .Values.addtasks.ports.targetPort }}
      
